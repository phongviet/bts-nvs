# Round-2 plan (2026-07-17) — supersedes phase-1 fleet targets

Round-2 test set = the ONLY submission target from now on. Scene facts:
`Analysis/ROUND2_test_set_analysis_2026-07-16.md`. Phase-1 public scenes
(hcm0034, HCM0181, ...) remain the LOCAL MEASUREMENT BENCH (only place with
GT); phase-1 private scenes are retired entirely.

## What carries over unchanged
- Grader metric + calibration (vgg LPIPS, psnr_max=50) — assumed same; verify
  against the first round-2 LB submission.
- Full stack: dense-init splatfacto-big → DIBR → per-scene refiner → encode.
- Wave levers (exp036/037/039/040/041): measured on the phase-1 bench,
  shipped on round-2 scenes.
- exp037 verdict already in (Jul-16): knapsack allocation LOSES; floor q95 and
  spend headroom. Shippable, ~+0.0004.

## What changes for round 2
1. **Conditional remap**: 5 drone scenes SIMPLE_RADIAL (k +0.0081..+0.0090,
   remap with per-scene k from cameras.bin); bonsai + chair SIMPLE_PINHOLE
   (k=0) — remap MUST be bypassed (identity resample only hurts).
2. **New indoor regime** (bonsai/chair): handheld video, 3–4° test-view gaps
   (prime DIBR), bonsai has motion blur + glossy reflective table, chair is
   PORTRAIT 720×1280 (verify no landscape assumptions).
3. **Layout shim**: `data/raw/round2/all/<scene>` symlinks →
   `VAI_NVS_DATA_ROUND2/`; `phase_run.sh <scene> all round2` works unchanged.
4. **Budget/packaging**: 386 test frames, mixed resolutions; builder needs a
   round-2 scene table (name → k, size, count) and per-scene remap toggle.
5. exp042 (3DGUT) de-prioritized: its motivating scene (HNI0131 k=−0.115) is
   retired; round-2 k's are mild.

## In flight (local wave, launched Jul-17 00:28, scripts/run_local_wave_2026-07-17.sh)
| step | what | exp |
|---|---|---|
| 10_chair | chair end-to-end (dense init → big-locked config → test renders) | round-2 |
| 21_encode_ab | equal-bytes knapsack A/B confirm | exp037 |
| 30–34 | DIBR K=5 + rel-τ sweep {5e-4,1e-3,2e-3}, traincheck + test | exp036 |
| 40–41 | flow-align (DIS backend) traincheck + test | exp039 |
| 50–51 | refiner seed-2 member + apply-time ensemble | exp036/R2 |
| 60_bonsai | bonsai end-to-end | round-2 |
| 70_refiner_v3 | NAFNet + evidence stack pilot | exp040 |

Note: DIBR/refiner pilots run on the BASE backbone variant (cached inputs) for
sign/magnitude; compose winners onto ss2+cubic+big before shipping (exp034
ladder practice). SEA-RAFT arm of exp039 still needs checkpoint + provenance
row (docs/rules_and_constraints.md) — DIS is the compliant default.

## Next after the wave
- Drone-scene fleet (5 scenes) on Kaggle with the locked stack + winning levers.
- First round-2 submission ASAP after chair/bonsai + 2 drone scenes validate
  the builder path (partial submissions scored fine in phase 1 — check round-2
  rules still allow).
- SSS backend verdict from the Kaggle paper-scale fleet (separate track,
  sss_experiment/).

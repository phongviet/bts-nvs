# Locked per-scene training config for `scripts/phase_run.sh` (make phase-run).
# FREEZE THIS at the Week-4 config freeze (exp017) -- it must reproduce the
# final Phase-1 fleet exactly, and it is what Phase 2/3 scenes run with on
# day 1. Current values = the Week-2 locked config (exp005); update after
# the exp006-010 read-out.
METHOD=splatfacto
MAX_ITERS=30000
EXTRA_ARGS="--pipeline.model.rasterize-mode antialiased"
# Dense-init build parameters (build_dense_colmap.py)
DENSE_MAX_POINTS=2000000
DENSE_MAX_IMAGE_SIZE=1600
# Set to 1 to build+use transient masks (exp008 gate decision)
USE_TRANSIENT_MASKS=0

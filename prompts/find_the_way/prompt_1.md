I have placed all of the results, source code, etc... for this field inside the results/ folder, README.md file (does also contain the results for model running). All of the source code is placed inside the markdown file representing each folder (I use tool to read all important files of that folder and then put all of them in the markdown file in the root of that sub-folder). For example, Analysis/ folder has Analysis.md. This makes you easier to read through them since you only have to index one file only.

The second thing is that we should be able to run it on RTX 4070S Ti 16GB of VRAM, with data loaded of phase 2 as guideline stated in the README.md

Main goal, main focus:
1. Understand docs/de_bai.md, docs/de_bai_submission_1.md as the problem that we have to solve. Think deeply, analyze source code, analyze results, read the text to find the explanation of why this model is not meeting its best performance. I want an increase in performance.
2. What you need is to find the gap in the model, which piece of data it mishandles without overfitting. We need to focus on real running, real debugging, real session. I want you to draft the pieces it is wrong, for me to find researches, methods, ... to fill in that gap, and improving the performance of the model without overfitting the test data. Try as much as you can as we are being in a hard competition. Identify which changes in params, how to increase model quality, you can suggest me if you need better GPU than a 4070S Ti with 16GB of VRAM. This is on CUDA 13.1, Python 3.10 with dependencies framing to environment.yml. 
3. Try as much as you can to debug without using too much of your time and computational resources. I want to debug as its root, as we already have the data, the basis results, just need to find the gap inside the model and increase it. You can treat me as an agent who knows how to read papers. Suggest me the keywords to find papers to fill in the gap, I find papers and their implementation to fill the gap, and then we try it for the existing solution. We are currently reaching 75.3 score, our goal is 77. Try your best. You are freely to debug on this Linux environment, with full power.